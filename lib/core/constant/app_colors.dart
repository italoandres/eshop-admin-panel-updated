import 'package:flutter/material.dart';

/// Cores do aplicativo
class AppColors {
  AppColors._();

  // Cores primárias
  static const Color primary = Color(0xFFFF4D67);
  static const Color primaryLight = Color(0xFFFFE5EC);
  
  // Cores de desconto
  static const Color discountBadge = primary;
  static const Color discountBackground = primaryLight;
  static const Color discountBorder = primary;
  static const Color discountText = primary;
}
