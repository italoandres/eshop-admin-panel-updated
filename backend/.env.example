# ==============================================
# CONFIGURAÇÕES ESSENCIAIS DO BACKEND
# ==============================================
# COPIE este arquivo para .env e configure os valores

# ========== SERVIDOR ==========
PORT=5000
NODE_ENV=development

# ========== MONGODB ==========
# Opção 1: MongoDB Atlas (recomendado para produção)
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/nomedb?retryWrites=true&w=majority

# Opção 2: MongoDB local (para desenvolvimento)
MONGODB_URI=mongodb://localhost:27017/eshop

# ========== JWT AUTENTICAÇÃO ==========
# IMPORTANTE: Gere uma chave secreta forte! Use: openssl rand -base64 32
JWT_SECRET=sua_chave_secreta_super_segura_aqui_trocar_em_producao
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=sua_chave_refresh_secreta_trocar_em_producao
JWT_REFRESH_EXPIRES_IN=30d

# ========== CLOUDINARY (Upload de Imagens) ==========
# Cadastre-se em: https://cloudinary.com (GRÁTIS até 25GB)
CLOUDINARY_CLOUD_NAME=seu_cloud_name
CLOUDINARY_API_KEY=sua_api_key
CLOUDINARY_API_SECRET=sua_api_secret

# ========== CORS (Frontend URLs permitidas) ==========
# Separe múltiplas URLs com vírgula
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# ========== CONFIGURAÇÕES OPCIONAIS ==========
# Rate limiting (requisições por minuto)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Admin token (para operações administrativas)
ADMIN_TOKEN=eshop_admin_token_2024

# ========== STORE ID (White-label) ==========
# ID padrão da loja (pode ser customizado)
DEFAULT_STORE_ID=eshop_001
STORE_NAME=EShop

# ========== CONFIGURAÇÕES DE PRODUÇÃO ==========
# Descomentar em produção:
# NODE_ENV=production
# SECURE_COOKIES=true
# TRUST_PROXY=true
